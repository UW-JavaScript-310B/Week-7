describe("Tests for Black Jack", () => {
  describe("Tests for card scenarios", () => {
    //– No aces
    it("Should return", () => {});
    //– 1 ace case (soft)
    it("Should return", () => {});
    //– 1 ace case (not soft)
    it("Should return", () => {});
    //– multipe ace case (soft)
    it("Should return", () => {});
    //– multipe ace case (not soft)
    it("Should return", () => {});
  });
});
