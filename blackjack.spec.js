describe("BlackJack Game", () => {
    describe("calcPoints Function", () => {
        const noAceHand = []
    // No aces
    it("should return ... if no aces", () => {
        const results = calcPoints(noAceHand);
        expect(result.total).toEqual(15)
        expect(result.isSoft).toEqual(false)
    });
    // 1acecase(soft)
    it("should return ...", () => {
        
    });
    // 1acecase(notsoft)
    it("should return ...", () => {
        
    });
    // multipeacecase(soft)
    it("should return ...", () => {
        
    });
    // multipeacecase(notsoft)
    it("should return ...", () => {
        
    });
    });
});